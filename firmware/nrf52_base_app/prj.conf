# Console / UART
CONFIG_SERIAL=y
CONFIG_UART_CONSOLE=y
CONFIG_CONSOLE=y
CONFIG_PRINTK=y
CONFIG_LOG=y
CONFIG_LOG_MODE_MINIMAL=y

# 緩解不必要的 Kconfig 中止
CONFIG_KCONFIG_WARNINGS_AS_ERRORS=n

# 核心/執行緒最小化
CONFIG_MAIN_STACK_SIZE=2048
CONFIG_HEAP_MEM_POOL_SIZE=0

# 不使用 POSIX 子系統，避免舊符號噪音
CONFIG_POSIX_API=n
