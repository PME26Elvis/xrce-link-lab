# 最小可行：不載平台、只創 PTY，輸出其路徑
mach create "pty-only"
emulation CreateUartPtyTerminal "pty0"

# 讓 CI 可以從 log 解析 PTY 路徑；Renode 的 UartPtyTerminal 也有 FileName 屬性
log "UART PTY: @{pty0.FileName}"

# 等一下，確保 log 寫出
emulation RunFor "0.5"
q
